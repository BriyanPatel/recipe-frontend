import AuthScreen from "./auth/auth-screen";


/**
 * This is the entry point of the app. It renders the AuthScreen, which handles
 * user authentication and authorization.
 *
 * @returns {JSX.Element} The AuthScreen component.
 */
export default function Home() {
  return (
    <AuthScreen />
  );
}
